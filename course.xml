<%namespace file="/course.xml" name="course"/>\
<course
  display_name="${course.display_name()}${course.suffix()}"
  url_name="${course.url_name()}"
  course="${course.course()}"
  org="${course.org()}">
</course>
